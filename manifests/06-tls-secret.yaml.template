# ⚠️ Ce fichier est un GABARIT. Ne pas commit tel quel.
# Le script scripts/01-generate-tls-cert.sh génère la vraie version
# (manifests/06-tls-secret.yaml) avec le certificat et la clé encodés en base64.
#
# Rappel pédagogique : dans un vrai projet, on NE versionne JAMAIS une clé privée
# en clair sur GitHub (même privé). Ici on le fait uniquement parce que :
#  - c'est un certificat auto-signé
#  - le projet est local/pédagogique (Minikube)
#  - le dépôt est supposé privé pour la démo
# En prod on utiliserait cert-manager + Let's Encrypt, ou un Sealed Secret / Vault.

apiVersion: v1
kind: Secret
metadata:
  name: alaina-tls-secret
  namespace: alaina
type: kubernetes.io/tls
data:
  tls.crt: <BASE64_DU_CERTIFICAT>
  tls.key: <BASE64_DE_LA_CLE_PRIVEE>
